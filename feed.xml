<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://raymondfan.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://raymondfan.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2025-04-01T13:06:44+00:00</updated><id>https://raymondfan.github.io/feed.xml</id><title type="html">blank</title><subtitle>A simple, whitespace theme for academics. Based on [*folio](https://github.com/bogoli/-folio) design. </subtitle><entry><title type="html">How Molecular Cascades Communicate</title><link href="https://raymondfan.github.io/blog/2025/info_cascades/" rel="alternate" type="text/html" title="How Molecular Cascades Communicate"/><published>2025-04-01T00:00:00+00:00</published><updated>2025-04-01T00:00:00+00:00</updated><id>https://raymondfan.github.io/blog/2025/info_cascades</id><content type="html" xml:base="https://raymondfan.github.io/blog/2025/info_cascades/"><![CDATA[<h2 id="introduction">Introduction</h2> <p>Information theory was originally developed in the context of optimizing electronic communications. This has resulted in a lot of confusion when applied to the biological sciences.</p> <p>The purpose of this blog post is to clarify what exactly information theory tells us about information transfer through a chemical cascade. It is intended as a supplement to <a href="https://arxiv.org/pdf/2309.10162" target="_blank">my paper on the topic</a>, written for an audience new to both information theory and stochastic modelling.</p> <p>Here, we consider analyze a cascade of three species of molecules, $X \xrightarrow{} Y \xrightarrow{} Z$, where $X \xrightarrow{} Y$ denotes that the production rate of molecule $Y$ is proportional to the amount of $X$ molecules, and similarly for $Y \xrightarrow{} Z$. This represents a simple biochemical cascade.</p> <p>Mathematically, the cascade is defined by the following chemical reactions</p> <p>\begin{aligned} y &amp;\xrightarrow[\phantom{y/\tau_y}]{\alpha x} y+1 &amp; <br/> y &amp;\xrightarrow[\phantom{y/\tau_y}]{y/\tau_y} y-1 &amp; <br/> z &amp;\xrightarrow[\phantom{z/\tau_z}]{\beta y} z+1 &amp; <br/> z &amp;\xrightarrow[\phantom{z/\tau_z}]{z/\tau_z} z-1 &amp; \end{aligned}</p> <p>where the dynamics governing $x(t)$ have been left unspecified. Such cascades have been used before to model relationships between DNA, mRNA and protein.</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <img src="/assets/img/blog_1/time_traces.png" class="img-fluid rounded z-depth-1" width="80%" height="auto" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> The chemical reactions can be simulated for some input $X(t)$ to generate $Y(t), Z(t)$. The process is fundamentally stochastic, resulting in different traces with each simulation. </div> <h2 id="information-theory-basics">Information Theory Basics</h2> <p>This section is a quick introduction to information theory concepts for non-experts. Feel free to skip if you are already familiar, although some of this formulation may make it easier to understand the latter portions.</p> <p>To communicate through our channel, we can choose an input signal $X(t)$, which in our cascade will generate curves $Y(t), Z(t)$. An observer watching the behavior of $Y(t), Z(t)$ can identify characteristics of the chosen input $X(t)$ and receive information.</p> <p>This idea is formalized in information theory by the following formula for mutual information between a pair of variables $X, Y$.</p> \[I(X; Y) = H(Y) - H(Y|X)\] <p>where $ H(Y) $ denotes the entropy of $ P(Y) $, while $ H(Y | X) $ is the entropy of the conditional distribution $ P(Y | X) $. The entropy is given by</p> \[H(X) = \sum_x P(x) log(P(x))\] <p>Entropy is an important concept in information theory, and measures spread of a distribution.</p> <p>To understand the idea behind this formula, consider a communication channel that returns $Y$ given $X$. In an ideal channel, every input $X$ maps to a unique output $Y$, and thus $H(Y | X) = 0$ Then mutual information is maximized at $I(X; Y) = H(Y)$. Most channels are noisy with $H(Y | X) &gt; 0$, lowering the information shared across this channel.</p> <p>The ‘noise’ also depends on the input distribution $P(X)$, as our channel will treat different inputs different, and some inputs may be noisier than others.</p> <p>This leads to concept of channel capacity. For a given channel, usually a physical process with an error in generating outputs $Y$ from inputs $X$, the channel capacity is the maximum $I(X; Y)$, achieved by maximizing over all possible distributions of input variables $P(X)$. It quantifies the maximum amount of information that can be sent through the channel.</p> <p>A useful property of mutual information is that it obeys the data processing inequality. The inequality states for a Markov chain $X \xrightarrow{} Y \xrightarrow{} Z$,</p> \[I(X; Z) \leq I(X; Y), I(Y; Z)\] <p>meaning mutual information between more distant variables in the chain is always less than mutual information between more closely related variables. Actually, this inequality is true as long as $P(X, Z |Y) = P(X | Y)P(Z | Y)$ (known as conditional independence), which is true for all Markov chains $X \xrightarrow{} Y \xrightarrow{} Z$.</p> <p>These properties allow us to analyze communication channels by computing the channel capacity of individual components. The data processing inequality tells us the channel capacity of the entire channel is bounded by the smallest channel capacity in the network.</p> <p>Note in the context of electrical communications, the channel capacity for a large network is achievable by transforming the data appropriately for each channel. But for a biological cascade where components are linked up directly and arbitrary transformations cannot be applied, the actual channel capacity may be much less than the individual components.</p> <h2 id="chemical-cascades-and-information">Chemical Cascades and Information</h2> <p>Our chemical cascade consists of physical processes which transform inputs $X$ into outputs $Y$ from a distribution $P(Y | X)$, and is thus a channel with nonzero error rate. The input to the cascade is a time-varying signal $X(t)$, and the output is a time-varying signal $Y(t), Z(t)$.</p> <p>For time-varying signals, channels are usually analyzed in terms of a rate of information transfer</p> \[R(X(t);Y(t)) = \frac{I(X(t); Y(t))}{D}\] <p>where $D$ denotes the duration over which we use our channel. The amount of information that can be conveyed through our channel increases linearly with the duration of usage $D$. This captures the idea that we can send arbitrarily large amounts of information through poor communication channels given enough time.</p> <p>To transfer information in our cascade, select an input time trace $X(t)$ of duration $D$. The physical processes of our channel convert the input into output time traces $Y(t), Z(t)$ with associated probabilities $P(Y(t) | X(t)), P(Z(t) | X(t))$. Channel quality is measured by rates $R(X(t);Y(t)), R(X(t);Z(t))$.</p> <p>For our chemical cascade, $P(Z(t) | Y(t), X(t)) = P(Z(t) | Y(t))$ since the time trace of $Z(t)$ only depends on $X(t)$ through how it affects $X(t)$ by definition, so the data processing inequality holds for the rates.</p> <h3 id="distinct-channels-in-our-cascade">Distinct Channels In Our Cascade</h3> <p>To determine information rates, note our cascade consists of four different physical processes that each generate new time traces given the preceding result as input.</p> <p>The first process is</p> <p>\begin{aligned} y \xrightarrow{\alpha x} y+1 \end{aligned}</p> <p>which from input time trace $X(t)$, generates a time trace $Y_{birth}(t)$ that tracks all the $y$ molecule production events that occur over the interval of usage $D$.</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <img src="/assets/img/blog_1/process_one.png" class="img-fluid rounded z-depth-1" width="90%" height="auto" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> Example input and output time traces for the channel given by the molecular production reaction. </div> <p>The second process is</p> <p>\begin{aligned} y \xrightarrow{y/\tau_y} y-1 \end{aligned}</p> <p>which uses $Y_{birth}(t)$ and an initial number of $y$ molecules, and generates the time trace $Y(t)$ using the random degradation of y molecules.</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <img src="/assets/img/blog_1/process_two.png" class="img-fluid rounded z-depth-1" width="90%" height="auto" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> Example input and output time traces for the channel given by the molecular degradation reaction. </div> <p>The other two physical processes are</p> <p>\begin{aligned} z &amp;\xrightarrow[\phantom{z/\tau_z}]{\beta y} z+1 &amp; <br/> z &amp;\xrightarrow[\phantom{z/\tau_z}]{z/\tau_z} z-1 &amp; \end{aligned}</p> <p>which generate $Z_{birth}(t)$ from $Y(t)$, and then $Z(t)$ using $Z_{birth}(t)$ births and an initial $Z(t = 0)$ value of molecules.</p> <h3 id="capacity-of-the-distinct-channels">Capacity Of The Distinct Channels</h3> <p>Our cascade consists of two physical processes, production events and degradation events. Here we discuss the capacity of these processes.</p> <p>The first process</p> <p>\begin{aligned} y \rightarrow{\alpha x} y+1 \end{aligned}</p> <p>corresponds to a well studied channel in the optical communications literature, known as the direct detection photon channel. Several results for the channel capacity are known under different constraints for $\alpha x$, which we briefly include here.</p> <details><summary>Channel Capacity Under Different Input Constraints</summary> <p>These results are taken from this <a href="https://static-content.springer.com/esm/art%3A10.1038%2Fnature09333/MediaObjects/41586_2010_BFnature09333_MOESM273_ESM.pdf">supplement on the topic</a>. If $\alpha x \leq A_{max}$, then it is known that the capacity is</p> \[C = A_{max} / e\] <p>If the mean $\alpha \langle x \rangle$ is also constrained, then the capacity becomes</p> \[C = \alpha \langle x \rangle ln\frac{A_{max}}{\alpha \langle x \rangle}\] <p>These capacities are realized by sending in $x(t)$ that oscillates between 0 and $A_{max}$ instantaneously. This is highly nonphysical for biological systems.</p> <p>The first capacity can be proven by breaking up the usage of the channel for duration $D$ into using the channel $N$ times each for a duration $D/N$. For large $N$ each use of the channel is equivalent to a Z channel with an error rate proportional to $N$, and taking the limit $N \xrightarrow{} \infty$ yields the capacity above. The optimal input time trace follows from the optimal inputs to the Z channel.</p> <p>Another constraint has been derived when the first two moments of the input signal are fixed, yielding</p> \[C = \alpha \langle x \rangle \log \left(1 + \frac{\sigma^2_f}{\langle x \rangle^2}\right) \leq \frac{\sigma^2_f}{\langle x \rangle}\] </details> <p>For the second process</p> <p>\begin{aligned} y \rightarrow{y/\tau_y} y-1 \end{aligned}</p> <p>which uses $Y_{birth}(t)$ and an initial number of molecules to generate $Y(t)$, note that $Y_{birth}(t)$ can always be inferred directly from $Y(t)$ by noting where the molecule number increases. Therefore the channel capacity of this process is always the entropy of the input $Y_{birth}$.</p> <p>Therefore the channel capacity of $X \rightarrow Y$ is simply the channel capacity of the production reaction, which has been studied before for various constraints.</p> <p>Conditional independence between $X(t), Z(t) | Y(t)$ thus implies the channel capacity of the full cascade is</p> \[C_{X\rightarrow Y \rightarrow Z} = \textbf{min}(C_{X\rightarrow Y }, C_{Y\rightarrow Z })\] <h2 id="existing-biological-experimental-data">Existing Biological Experimental Data</h2> <p>In practice, it is difficult to measure time traces of multiple molecules inside of living cells simultaneously. Furthermore, the mutual information discussed above applies to distributions of time traces, which is hard to estimate from experimental data (any specific time trace has near zero probability).</p> <p>However, it is possible to measure the abundances of multiple molecules in recently deceased cells. This can be done on large numbers of cells to obtain joint probability distributions of <b>instantaneous</b> molecular abundances. This data can be analyzed to infer properties of the underlying relationships. This is illustrated in the following figure.</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <img src="/assets/img/blog_1/full%20process.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> The chemical reactions inside of a living cell result in a channel of input and output time traces. However, typical experiments only measure instantaneous molecular abundances, which when repeated for many different cells produce joint distributions of instantaneous abundances. These instantaneous abundances do not form a channel with respect to each other. </div> <p>Note instantaneous joint distributions do not obey the same properties as the underlying joint distributions of time traces. Instead, they are derived from the time trace distribution through randomly sampling the time traces at the same time.</p> <p>Such joint distributions can still contain useful information theoretic properties. Consider the probability distribution of time traces $P(X(t))$. From this we derive both the sequence of birth events $Y_{birth}$ (and thus the time trace $Y(t)$), and the probability distribution of instantaneous $X$ values. Thus we find $X, Y(t)$ are conditionally independent of each other given $X(t)$.</p> <p>However, the link between the different instantaneous distributions only arises from averaging the related time traces. The different instantaneous values of $X, Y, Z$ do not form a proper communication channel $X \rightarrow Y \rightarrow Z$, and thus $X, Z$ are not conditionally independent of each other given $Y$ - they become independent when conditioned on $X(t), Y(t)$ or $Z(t)$.</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <img src="/assets/img/blog_1/markov_chain.png" class="img-fluid rounded z-depth-1" width="80%" height="auto" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> One source of confusion in the literature is the notion of a Markov chain. Our system of chemical reactions does form a Markov chain, but it is not a Markov chain in the sense that would be needed to have the data processing inequality between the experimentally observed distributions. </div> <h3 id="violations-of-the-data-processing-inequality">Violations Of The “Data-Processing Inequality”</h3> <p>A consequence of analyzing the static distributions from experiments is that the data processing inequality which states that for a channel $X \rightarrow Y \rightarrow Z$, the mutual information $I(X; Z) \leq I(X; Y), I(Y; Z)$ is no longer true (since these distributions do not form a channel).</p> <p>This allows for biological cascades to generate distributions in experiments that violate the inequality. This was covered in detail in the paper, where mathematics and numerical simulations have been used to explore the regime of parameter space for which these inequalities break down. This is illustrated in the following figure, where simulating a cascade for a variety of parameters reveals regions of parameter space for which the mutual information $I(X; Z) &gt; I(X; Y)$ (blue region).</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <img src="/assets/img/blog_1/mutual_information_figure.png" class="img-fluid rounded z-depth-1" width="80%" height="auto" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> Simulating the cascade for a $X(t)$ that obeys a poissonian birth-death process for a wide variety of parameters allows us to obtain the joint distributions of the instantaneous abundances, $P(X, Y, Z)$. Calculating the mutual information for these joint distributions for a wide range of parameters reveals regimes where the inequalities suggested by the data processing inequality are violated (blue region). See the original paper for more details. </div> <p>For a simple intuitive explanation of how this can occur, consider the problem of estimating the instantaneous value of $X$. Suppose the abundance of $Y$ is very small, such that low copy number noise is very significant. Then for any value of $X$, there is a good chance of a large variety of $Y$ values, and it is difficult to estimate $X$ from $Y$.</p> <p>Suppose that $Y$ molecules have a very short lifetime compared to $X, Z$ molecules. Then the abundance $Y(t)$ will oscillate very quickly. $Z$ molecules, which are made proportional to the amount of $Y$ molecules, will experience rapid fluctuations in their birth rate. But the $Z$ abundance is related to the number of $Z$ molecules made over the average lifetime of a $Z$ molecule, and thus does not vary as much if the $Z$ lifetime is large compared to the $Y$ molecule lifetime.</p> <p>In this way, we can accurately estimate the current value of $X$ by looking at the $Z$ abundance, since $Z$ time-averages out the large amount of $Y$ noise. This idea of estimation is closely linked with the mutual information (which represents the reduction in uncertainty about the value of $X$.</p> <h2 id="on-actual-inequalities">On Actual Inequalities</h2> <p>In this post, we’ve seen how the data processing inequality applies to chemical cascades only when considering the distribution of time traces.</p> <p>The often experimentally measured instantaneous distributions of molecules however, do not obey these inequalities because they do not form a Markov Chain with respect to each other, and there exist parameter regimes where even simple cascades can display apparent ‘violations’.</p> <p>This observation prompts a deeper question:</p> <blockquote> Can we derive <b>new inequalities</b> that apply to these experimentally accessible (static) distributions? </blockquote> <p>Surprisingly, <b>yes</b>—but with caveats.</p> <p>While we saw that $Y$ can accumulate arbitrary information from $X$ over time, accurate information about the current state of $X$ is different, as older information becomes irrelevant. There is a tradeoff between:</p> <ul> <li>The <b>rate</b> at which $Y$ learns about $X$ (governed by the production reaction)</li> <li>The <b>rate</b> at which $X$’s state changes (governed by X’s dynamics)</li> </ul> <p>Unlike the DPI which holds always, these new inequalities must be dependent on the dynamics of the system involved and do not generalize. Furthermore, they’re mathematically difficult to derive and work with. I’ll discuss more on this topic in my next blog post, on <b>“Inequalities for Static Distributions in Biochemical Cascades”</b>.</p>]]></content><author><name>Raymond Fan</name></author><category term="distill"/><category term="formatting"/><summary type="html"><![CDATA[Clarifying a common misunderstanding when applying information theory to biological data]]></summary></entry></feed>