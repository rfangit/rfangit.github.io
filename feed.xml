<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://raymondfan.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://raymondfan.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2025-06-03T22:07:38+00:00</updated><id>https://raymondfan.github.io/feed.xml</id><title type="html">blank</title><subtitle>A simple, whitespace theme for academics. Based on [*folio](https://github.com/bogoli/-folio) design. </subtitle><entry><title type="html">Introduction to Mean Flow</title><link href="https://raymondfan.github.io/blog/2025/intro_mean_flow/" rel="alternate" type="text/html" title="Introduction to Mean Flow"/><published>2025-06-02T00:00:00+00:00</published><updated>2025-06-02T00:00:00+00:00</updated><id>https://raymondfan.github.io/blog/2025/intro_mean_flow</id><content type="html" xml:base="https://raymondfan.github.io/blog/2025/intro_mean_flow/"><![CDATA[<p>This tutorial is implemented as a <a href="https://colab.research.google.com/drive/18HeOrhQ_5u-TvHhfxHr8_t_03pX-tHO-" target="_blank">google colab notebook</a>, where you can follow along with the code and run it yourself.</p> <h2 id="introduction">Introduction</h2> <p>“Mean Flows for One-step Generative Modeling” is a new idea to modify flow matching for 1 step generation. In this tutorial post, we’ll introduce the basics and an example implementation for standard 2d toy flow matching problems. Do consider reading the original paper for more details.</p> <h2 id="flow-matching">Flow Matching</h2> <p>In flow matching, we train a model to learn a flow to transform a source distribution into a target distribution. This model is trained by sampling random points from both distributions along with random times. Our model will learn the average direction to flow for a given $\vec{x}(t), t$, where $\vec{x}(t)$ represents our current data at time t.</p> <p>Unfortunately this flow is fairly curved. As a result, transforming source to target requires evaluating our model at many points.</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <img src="/assets/img/blog_4/model_fig.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> Figure 1: Illustration of the flow matching process, visualizing training data on top and learned model outputs on the bottom. Due to the curvature of the model flow, many evaluations are needed to transform source into target. </div> <h2 id="mean-flow">Mean Flow</h2> <p><strong>Note:</strong> We follow the convention in the original paper, where $t = 0$ corresponds to our data, and $t = 1$ corresponds to the noise.</p> <p>Instead of training a model to predict the flow at a point $v(\vec{x}(t), t)$, mean flow modifies the training algorithm and model to predict the average flow over the interval $r, t$ given by</p> <p>\begin{equation} u(\vec{x}(t), t, r) = \frac{1}{t - r} \int_{r}^{t} v(\vec{x}(s), s) ds \end{equation}</p> <p>This corresponds to averaging the flow over the green line in the following diagram.</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0" style="max-width: 600px; margin: 0 auto;"> <figure> <picture> <img src="/assets/img/blog_4/meanflow.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> Figure 2: Illustration of mean flow. Mean flow is a method to train a model to predict the average velocity along a trajectory, allowing one to accurately numerically integrate over the interval $[r, t]$ in a single step. </div> <p>The mean flow is useful in generating with less steps, since the model already averages over $r, t$. 1-step sampling for example can be accomplished by setting $r = 0, t = 1$, and using the model’s mean flow to instantly generate outputs from inputs.</p> <p>\begin{equation} \vec{x}(t = 0) = \vec{x}(t = 1) - u(\vec{x} (t = 0), r = 0, t = 1) \end{equation}</p> <p>Note that mean flow models can still be used for multi-step generation.</p> <p>To train a mean flow model, we modify the model to accept an extra time parameter $r$ (the starting time for our velocity averaging), and modify our loss function.</p> <h2 id="modified-loss">Modified Loss</h2> <p>Taking the derivative with respect to $t$ (treating r as a constant), we get</p> \[\begin{aligned} &amp;\frac{d}{dt} \left( (t - r) u(\vec{x}(t), r, t) \right) = \frac{d}{dt} \int_r^t v(\vec{x}(s), s)\, ds \\&amp; \implies u(\vec{x}(t), r, t) = v(\vec{x}(t), t) - (t - r) \frac{d}{dt} u(\vec{x}(t), r, t) \end{aligned}\] <p>The time derivative can be computed in neural networks easily thanks to automatic differentiation.</p> <p>Specifically, pytorch has a function called jvp (standing for jacobian vector product), which takes the product of the derivatives of a function with a vector. JVP can compute the derivative, with one issue: in $\frac{d}{dt} u(\vec{x}(t), r, t)$, our model depends on $t$ explicitly in its third argument, and implicitly in its first argument $\vec{x}(t)$.</p> <p>JVP only computes explicit derivatives, so we need to make the $t$ dependence apparent. This yields</p> <p>\begin{equation} \frac{d}{dt} u(\vec{x}(t), r, t) = v(\vec{x} (t), t) \times \frac{\partial}{\partial \vec{x}} u(\vec{x}(t), r, t) + \frac{\partial}{\partial t} u(\vec{x}(t), r, t) \end{equation}</p> <p>which is the jacobian vector product of our model $u(\vec{x}(t), r, t)$ with the vector $v, 0, 1$.</p> <h2 id="mean-flow-code">Mean Flow Code</h2> <p>That covers the theory in mean flows. In practice, we make 3 modifications to standard flow matching. In flow matching:</p> <ul> <li>Random t values are generated in training.</li> <li>Loss is computed by comparing the model to the sample velocity</li> <li>Sampling occurs by evaluating the model at time t and incrementing forward by dt.</li> </ul> <p>In mean flow:</p> <ul> <li>Random $t, r$ values are generated in training, with $t &gt; r$.</li> <li>Loss is computed with the sample velocity modified by a jacobian term.</li> <li>Sampling occurs by evaluating the model at $t = t_{current}, r = t - dt $. For single step sampling, we evaluate at $r = 0, t = 1$.</li> </ul> <p>To accomplish this, we define</p> <ul> <li>a model that accepts an additional time parameter $r$. For 2D examples, a couple fully connected layers suffice, and it’s easy to add the extra time dimension.</li> <li>a class for handling $t, r$ generation that computes the loss.</li> <li>a training function that uses our pre-existing class.</li> </ul> <p><strong>Note:</strong> Pay careful attention to loss computation! If you don’t use detach() to stop the flow of gradients, it’s very easy to get a model that never trains.</p> <div class="language-markdown highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="gh"># Mean Flow Model</span>
class MeanFlowNet(nn.Module):
    def __init__(self, input_dim, h_dim=64):
        super().__init__()
        # Input dimension should be x (input_dim) + t (1) + r (1) = input_dim + 2
        self.fc_in  = nn.Linear(input_dim + 2, h_dim)
        self.fc2    = nn.Linear(h_dim, h_dim)
        self.fc3    = nn.Linear(h_dim, h_dim)
        self.fc4    = nn.Linear(h_dim, h_dim)
        self.fc_out = nn.Linear(h_dim, input_dim)<span class="sb">

    def forward(self, x, t, r, act=F.gelu):
        t = t.expand(x.size(0), 1)  # Ensure t has the correct dimensions for x batches
        r = r.expand(x.size(0), 1)  # Add r for meanflow!

        x = torch.cat([x, t, r], dim=1)
        x = act(self.fc_in(x))
        x = act(self.fc2(x))
        x = act(self.fc3(x))
        x = act(self.fc4(x))
        return self.fc_out(x)

</span><span class="gh"># MeanFlow class that handles time generation, loss computation</span>
class MeanFlow:
    def __init__(self,):
        super().__init__()<span class="sb">

    def sample_t_r(self, batch_size, device):
        # Generate random t values in the shape of the batch size
        samples = torch.rand(batch_size, 2, device=device)

        # Assign the smaller values to r, larger values to t, unsqueeze to make it fit the 2D data
        t = torch.max(samples[:, 0], samples[:, 1]).unsqueeze(1)
        r = torch.min(samples[:, 0], samples[:, 1]).unsqueeze(1)
        return t, r

    def loss(self, model, target_samples, source_samples):
        batch_size = target_samples.shape[0]
        device = target_samples.device

        t, r = self.sample_t_r(batch_size, device) # Generate t, r

        interpolated_samples = (1 - t) * target_samples + t * source_samples
        velocity = source_samples - target_samples # velocity takes targets to sources

        ## Mean Flow Specific Loss Calculation ##
        jvp_args = (model,(interpolated_samples, t, r),(velocity, torch.ones_like(t), torch.zeros_like(r)), )
        u, dudt = jvp(*jvp_args, create_graph=True)
        u_tgt = velocity - (t - r) * dudt

        ## NOTE: Very important to use .detach().
        ## This sort of gradient based loss is very unstable otherwise
        ## and models can get stuck in extremely terrible local minima!!
        loss = F.mse_loss(u, u_tgt.detach()) ## Default MSE Loss
        return loss

</span><span class="gh"># Training function that uses the class</span>
def train_mean_model(model, source_data_function, target_data_function, n_epochs=100, lr=0.003, batch_size=2048, batches_per_epoch=10, epoch_save_freq = 10, checkpoint_prefix='mean_flow_model'):
    optimizer = optim.Adam(model.parameters(), lr=lr)#torch.optim.AdamW(model.parameters(), lr=lr, weight_decay=0.0) #different optimizer
    device = next(model.parameters()).device
    #define an instance of meanflow to use to handle times and loss calculation<span class="sb">

    meanflow = MeanFlow()

    for epoch in range(n_epochs):
        model.train()
        total_loss = 0.0
        for batch_idx in range(batches_per_epoch):
            # obtain points
            source_samples = source_data_function(batch_size).to(device)
            target_samples = target_data_function(batch_size).to(device)

            # Use points in the meanflow class
            loss = meanflow.loss(model, target_samples, source_samples)

            loss.backward()
            optimizer.step()
            optimizer.zero_grad()

            total_loss += loss.item()
        avg_loss = total_loss / batches_per_epoch
        print(f"Epoch [{epoch}/{n_epochs}], Avg Loss: {avg_loss:.4f}")

        if epoch % epoch_save_freq == 0:
            # Save model checkpoint
            checkpoint_path = f'{checkpoint_prefix}_epoch_{epoch}.pt'
            torch.save({
                'epoch': epoch,
                'model_state_dict': model.state_dict(),
                'optimizer_state_dict': optimizer.state_dict(),
                'loss': loss.item(),
            }, checkpoint_path)
            print(f"Saved model checkpoint to {checkpoint_path}")
    # Always save final model at the end
    checkpoint_path = f'{checkpoint_prefix}_epoch_{n_epochs}.pt'
    torch.save({
        'epoch': n_epochs,
        'model_state_dict': model.state_dict(),
        'optimizer_state_dict': optimizer.state_dict(),
        'loss': loss.item(),
    }, checkpoint_path)
    print(f"Saved model checkpoint to {checkpoint_path}")
    return model
</span></code></pre></div></div> <p>And that’s it!</p> <h2 id="example---2d">Example - 2D</h2> <p>We’ll demonstrate mean flow on several 2D distributions.</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <img src="/assets/img/blog_4/2d_dists.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> Figure 3: Example 2D distributions used in this post. </div> <p>We’ll start with the checkerboard distribution.</p> <h3 id="flow-matching-example">Flow Matching Example</h3> <p>As a comparison, we first train a flow matching model. We define a model and a flow matching training function that does not use our earlier class. After training, we display 1 step integration as well as multi-step integration, using a simple forward euler to integrate our flow. The target distribution is shown in blue.</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <img src="/assets/img/blog_4/checkerboard_flow_match.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> Figure 4: Results for flow matching with the checkerboard distribution. </div> <p>Our flow matching model generates accurate results using multiple denoising steps, but performance on single step generation is poor.</p> <h3 id="mean-flow-example">Mean Flow Example</h3> <p>Next, mean flow.</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <img src="/assets/img/blog_4/checkerboard_mean_flow.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> Figure 5: Results for mean flow with the checkerboard distribution. </div> <p>Mean flow models have good 1-step generation results (especially compared to the original flow matching models, which will never generate good 1-step generation by minimizing the loss function: see <a href="https://rfangit.github.io/blog/2025/optimal_flow_matching/">this post</a> for details). For the checkerboard distribution, the quality of multi-step generation is slightly worse for mean flow even with more training than flow matching, which appears to be a general drawback of mean flow models.</p> <h3 id="other-2d-distributions">Other 2D Distributions</h3> <p>Here, we compare flow matching and mean flow across a variety of distributions.</p> <h4 id="crescent">Crescent</h4> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <img src="/assets/img/blog_4/crescent_mean_flow.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <img src="/assets/img/blog_4/crescent_flow_match.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> Figure 5: Results for crescent shaped distributions.. </div> <h4 id="spiral">Spiral</h4> <p>Note this distribution is the most difficult to learn for both mean flow and flow matching.</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <img src="/assets/img/blog_4/spiral_mean_flow.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <img src="/assets/img/blog_4/spiral_flow_match.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> Figure 5: Results for spiral distributions. </div> <h2 id="results">Results</h2> <p>Across the 3 distributions considered here, we find excellent results for 1 step generation compared to flow matching. The multi-step generation quality is usually similar to flow matching, with the mean flow results arguably lower in quality even with more training time.</p> <p>The most striking diferences were for the spiral distribution, which is the hardest for both models to learn. Flow-matching is able to achieve notably superior performance on multi-step generation with less training (not the large green regions in the mean flow that do not exist in the original spiral).</p> <h2 id="overall-thoughts">Overall Thoughts</h2> <p>Mean flows are an interesting method for 1 step generative modelling. Hopefully this post helps in understanding how they work, and gives a good feel for how its results as compared to standard flow matching.</p> <p>For my personal (limited) experience with this framework in 2D toy settings:</p> <ul> <li>1-step results are very impressive.</li> <li>Multi-step generation results are comparable to flow matching, but tend to be lower quality.</li> <li>Models take more epochs to train than flow matching, and the time per epoch is larger thanks to the complex loss function.</li> </ul> <p>Presumably these drawbacks remain the same in more complex systems of interest. They have the ability to offer high quality single-step generation, at the cost of multi-step generation and increased training time.</p> <p>If you happen to notice any mistakes or bugs, please contact me and I’ll fix it. Thanks!</p>]]></content><author><name>Raymond Fan</name></author><category term="code"/><category term="math"/><category term="artificial-intelligence"/><summary type="html"><![CDATA[This tutorial is implemented as a google colab notebook, where you can follow along with the code and run it yourself.]]></summary></entry><entry><title type="html">Optimal Flow-Matching</title><link href="https://raymondfan.github.io/blog/2025/optimal_flow_matching/" rel="alternate" type="text/html" title="Optimal Flow-Matching"/><published>2025-05-11T00:00:00+00:00</published><updated>2025-05-11T00:00:00+00:00</updated><id>https://raymondfan.github.io/blog/2025/optimal_flow_matching</id><content type="html" xml:base="https://raymondfan.github.io/blog/2025/optimal_flow_matching/"><![CDATA[<p>The implementation of the solution for various datasets are available on <a href="https://github.com/rfangit/analytical_flow_matching/" target="_blank">github.</a></p> <h2 id="introduction">Introduction</h2> <p>The training objectives of diffusion models can be solved analytically for an arbitrary dataset, yielding an algorithm that optimally denoises images in a way that minimizes the loss without any training. <d-cite key="karras2022elucidatingdesignspacediffusionbased"></d-cite> However, the analytical solution has two undesirable properties:</p> <ul> <li> <p>Removing the noise at a single step requires iterating through the entire dataset. This is computationally expensive.</p> </li> <li> <p>The algorithm only generates images present in the training set. It memorizes and cannot produce new images.</p> </li> </ul> <p>However, this algorithm may be useful for understanding how deep learning models trained with the diffusion objective are producing new images.</p> <p>Flow-matching is a new formulation of generative modeling using the language of ODE flows. <d-cite key="lipman2023flowmatchinggenerativemodeling"></d-cite> It is equivalent to diffusion, but conceptually and mathematically simpler. Here, we derive the solution that minimizes the flow-matching objective’s loss for an arbitrary dataset.</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <img src="/assets/img/blog_3/checkerboard_example.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> A common toy example in flow-matching is to match a 2D isotropic Gaussian (blue) to a checkerboard distribution (red). </div> <h2 id="flow-matching">Flow-Matching</h2> <p>In flow-matching, we randomly sample points $\vec{x_{i}}, \vec{x_{f}}$ from the input and output distributions, and calculate a sample velocity</p> \[\vec{v}_{s} = \vec{x_i} \alpha' (t) + \vec{x_f} \beta' (t)\] <p>where $t$ is a randomly chosen time $t \varepsilon [0, 1]$, and $\alpha (t), \beta (t)$ are scheduling functions with $\alpha (0) = \beta (1) = 1, \alpha (1) = \beta (0) = 0$. Common choices are $\alpha(t) = 1 - t, \beta(t) = t$.</p> <p>We train a model $\vec{v}_{m}(\vec{x}, t)$ to predict the sample velocity given the position</p> \[\vec{x} = \vec{x}_i \alpha(t) + \vec{x}_f \beta(t)\] <p>Our model minimizes the MSE loss</p> \[L = E_{\vec{x_{i}}, \vec{x_{f}}, t} \left[ \vec{v}_{m} (\vec{x}, t) - \vec{v}_{s})^2 \right]\] <p>where the expected value is taken over the distribution of inputs and outputs $\vec{x_{i}}, \vec{x_{f}}$.</p> <p>The MSE loss is minimized if our model predicts the average sample velocity for a given $\vec{x}, t$</p> \[\implies \vec{v}_{m} (\vec{x}, t) = \langle \vec{v_{s}} \rangle_{\vec{x}, t}\] <p>which can be calculated by integrating over the distribution of inputs and outputs $P(\vec{x_i}, \vec{x_f})$ conditioned on points $\vec{x_i}, \vec{x_f}$ contributing to the velocity at point $\vec{x}, t$. The average velocity calculation involves integrals of the following form</p> \[\langle x_{i} \rangle_{\vec{x}, t} = \frac{\int_{\vec{x_i}, \vec{x_f}} \vec{x}_{i} \, P( \vec{x_i}, \vec{x_f} \mid \vec{x} = \vec{x_i} \alpha (t) + \vec{x_f} \beta (t)) \, dx_i dx_f}{\int_{\vec{x_i}, \vec{x_f}} \, P( \vec{x_i}, \vec{x_f} \mid \vec{x} = \vec{x_i} \alpha (t) + \vec{x_f} \beta (t)) \, dx_i dx_f}\] <p>where we condition the distribution of inputs and outputs on points contributing to the velocity $\langle \vec{v_{s}} \rangle_{\vec{x}, t}$. The denominator ensures the conditional probability distribution is appropriately normalized (since only connected pairs $\vec{x}_i, \vec{x}_f$ contribute to a given $\vec{x}, t$).</p> <p>These integrals can be simplified if we impose constraints on the distributions (eg, our inputs are Gaussian noise).</p> <h2 id="optimal-solution">Optimal Solution</h2> <p>If the input distribution is a Gaussian, and the output distribution is defined by discrete training data points $\vec{\mu}_j$, the velocity that minimizes our training objective is given by</p> \[\vec{v} (\vec{x}, t) = \frac{1}{1 - t}\left( \sum_j \vec{\mu}_{j} \cdot \text{Softmax}_{\vec{\mu}_j}\left(-\frac{1}{2} \frac{\left(\vec{x} - \vec{\mu}_{j} t \right)^2}{(1 - t)^2 \sigma_i} \right) - \vec{x} \right)\] <p>where we have additionally assumed the schedule is linear. $\text{Softmax}_{\vec{\mu}_j}$ refers to the output of the softmax function, given by</p> \[\text{Softmax}_{\vec{x}} = \frac{e^{\vec{x}}}{\sum_{\vec{x}} e^{\vec{x}}}\] <p>For the derivation, see this <a href="https://github.com/rfangit/analytical_flow_matching/blob/main/Optimal_Flow_Matching___Appendix.pdf" target="_blank">appendix.</a></p> <p>This algorithm is easy to interpret. At any time, the ideal flow checks the $L_2$ distance between the current point and all training data points, multiplied by a time dependent factor. The softmax turns this into a distribution, and it moves directly to the expected endpoint $\langle \mu \rangle$ of this distribution. The $1/(1 - t)$ term means that single-step integration from this point takes it directly to this expected end point.</p> <p>The two drawbacks of ideal diffusion denoisers are present here:</p> <ul> <li> <p>Evaluating the velocity involves computing distances with all data points, which is computationally expensive for large datasets.</p> </li> <li> <p>It memorizes the training data. As $t \rightarrow 1$, the velocity points directly to the closest data point (as the softmax penalizes more distant points). As a result, it only generates points at $t = 1$ that exist in the training data.</p> </li> </ul> <p>Despite this, such an algorithm may be helpful in interpreting how denoising generative models behave. For example, at $t = 0$, all our data points become identical and flows always move to the mean of the training data, which can be easily confirmed. As $t \rightarrow 1$, our flows begin to distinguish different data points and more fine features become pronounced.</p> <h2 id="examples-with-code">Examples With Code</h2> <p>This algorithm has been implemented in this <a href="https://github.com/rfangit/analytical_flow_matching/" target="_blank">github repo with example uses.</a> Here we display optimal flows using as training data the first 50,000 images of MNIST, CIFAR-10, and 5000 images from ImageNet.</p> <div class="row"> <div class="col-md-4"> <figure> <picture> <img src="/assets/img/blog_3/mnist_analytic_denoising_seed.gif" class="img-fluid" width="100%" height="auto" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">MNIST</figcaption> </figure> <div class="mt-3"> <figure> <picture> <img src="/assets/img/blog_3/cifar_analytic_denoising_seed.gif" class="img-fluid" width="100%" height="auto" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">CIFAR-10</figcaption> </figure> </div> </div> <div class="col-md-8"> <figure> <picture> <img src="/assets/img/blog_3/image_net_analytic_denoising.gif" class="img-fluid" width="100%" height="auto" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">ImageNet</figcaption> </figure> </div> </div> <div class="caption"> Examples of the analytic denoiser on Gaussian noise for MNIST, CIFAR-10, and Imagenet. </div> <p>With this algorithm, one can denoise images very quickly for small datasets, but the resulting images are nearly identical to training examples. It’s incapable of generating new images.</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0" style="max-width: 600px; margin: 0 auto;"> <figure> <picture> <img src="/assets/img/blog_3/generated_checks.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> Although the ideal denoiser requires no training, all outputs are identical to images in the training data. </div> <p>Since the ideal flow minimizes the MSE loss, one can compute the minimal training loss. This seems most enlightening when plotted as a function of $t$, where one observes that the loss is maximal near $t = 0$ and steadily decreases.</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0" style="max-width: 600px; margin: 0 auto;"> <figure> <picture> <img src="/assets/img/blog_3/high_d_losses.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> Optimal MSE loss at various parameters of $t$ for training MNIST and CIFAR-10. The loss at high values of $t$ is small since contributions to the velocity come mostly from a single sample, which the optimal denoiser has memorized. Losses were calculated using $400$ randomly generated points at each value of $t$. </div> <p>This occurs because there is greatest variance in the velocity at the start, where all output images seem possible. As time progresses, very few images are likely and the softmax becomes dominated by a specific training example.</p> <p>This can be seen explicitly when plotting our algorithms prediction for the final image at a given time, where they initially predict a blurry image corresponding to the average but very quickly decide on the final image. More training data should ensure the variance stays large for larger $t$ values.</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0" style="max-width: 500px; margin: 0 auto;"> <figure> <picture> <img src="/assets/img/blog_3/predictions.png" class="img-fluid rounded z-depth-1" width="80%" height="auto" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> Optimal denoiser predictions for the final image for MNIST and CIFAR-10. Note that the optimal algorithm initially predicts the average of the training data, but quickly decides on which of the memorized samples corresponds to the input. </div> <p>This behavior is very different for toy 2D distributions often used to visualize flow-matching algorithms such as circles, checkerboards or spiral distributions, where the loss is non-monotonic.</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0" style="max-width: 600px; margin: 0 auto;"> <figure> <picture> <img src="/assets/img/blog_3/loss_2d_dists.png" class="img-fluid rounded z-depth-1" width="70%" height="auto" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> Optimal MSE loss at various parameters of $t$ for training common 2D toy distributions (matching a Gaussian to a circle or a checkerboard). Unlike the high dimensional problems earlier, the loss behaves non-monotonically. Losses were calculated using $40,000$ randomly generated points at each value of $t$. </div> <h2 id="what-do-models-actually-do">What Do Models Actually Do?</h2> <p>For networks trained on large amounts of data, it is unlikely they have the capacity in their weights to store all their training data. Although this algorithm minimizes the loss, it cannot describe the behavior of networks except in the memorization regime that occurs with a small number of samples. <d-cite key="ambrogioni2023searchdispersedmemoriesgenerative"></d-cite></p> <p>There exists a large body of work trying to understand how these models behave outside of the memorization regime, such as the appearance of geometric harmonic bases <d-cite key="kadkhodaie2024generalizationdiffusionmodelsarises"></d-cite> , and translational invariance and locality inherited from convolutional neural networks. <d-cite key="kamb2024analytictheorycreativityconvolutional"></d-cite></p> <p>Here we focus on small insights from the optimal solution. For neural networks, the problem is estimating</p> \[\sum_j \vec{\mu}_{j} \cdot \text{Softmax}_{\vec{\mu}_j}\left(-\frac{1}{2} \frac{\left(\vec{x} - \vec{\mu}_{j} t \right)^2}{(1 - t)^2 \sigma_i} \right)\] <p>given a finite capacity.</p> <p>Near $t = 0$, this term is easy to approximate since all data points $\vec{\mu}_{j} t \rightarrow 0$ and the flow points to the average. Only small corrections to the average-tending flow are needed and the error will be small as long as $t$ is small.</p> <p>On the other hand, $t = 1$ appears difficult to approximate since all data points have separated and the softmax is dominated by the closest point, for which there are many candidates. The resolution to this paradox is that $\vec{x}$ near $t = 1$ is not random noise, but very close to the training data $\vec{\mu}_j$. Therefore our model can use cues present in $\vec{x}$ at this step to make a good guess towards the expected value of the softmax, or direction of flow.</p> <p>What these cues are and how the model learns them do not seem explainable from the algorithm, but rather dependent on features of the data and model architecture.</p> <h2 id="generalizations">Generalizations</h2> <p>For an arbitrary schedule where some data points are more likely than others, we have</p> \[\vec{v} (\vec{x}, t) = \frac{\alpha'}{\alpha} \vec{x} + \frac{\alpha \beta' + \alpha' \beta}{\alpha} \sum_j \vec{\mu}_{j} \cdot \text{Softmax}_{\vec{\mu}_j}\left(-\frac{1}{2} \frac{\left(\vec{x} - \vec{\mu}_{j} \beta \right)^2}{\alpha^2 \sigma_i} + \log C_j \right)\] <p>where $\alpha’, \beta’$ are derivatives of the scheduling functions, and $C_j$ is a constant that weighs the relative probability of certain data points in the training data.</p> <p>If the output distribution consists of a superposition of isotropic Gaussians with variance $\sigma_f$, then</p> \[\begin{aligned} \vec{v}(\vec{x}, t) &amp;= \left(\frac{1}{\alpha^2 \sigma_i + \beta^2 \sigma_f} \right) \cdot \bigg[ \vec{x} \left(\alpha' \alpha \sigma_i + \beta' \beta \sigma_f \right) \\ &amp;\quad - \alpha \sigma_i\left(\alpha' \beta - \alpha \beta' \right) \left( \sum_j \vec{\mu}_{j} \cdot \text{Softmax}_{\vec{\mu}_j}\left(-\frac{1}{2} \frac{\left(\vec{x} - \vec{\mu}_{j} \beta \right)^2}{\alpha^2 \sigma_i + \beta^2 \sigma_f} + \log C_j \right) \right) \bigg] \\ \end{aligned}\] <p>which represents a case where Gaussian noise is added to our training data.</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0" style="max-width: 480px; margin: 0 auto;"> <figure> <picture> <img src="/assets/img/blog_3/flow_matching_analytic_line.gif" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> Optimal flow-matching from a 2D Gaussian to a series of points on a line with a nonlinear schedule $\alpha = 1 - t^{2}, \beta = t^{2}$ and allowing for variance in the output $\sigma_f &gt; 0$. </div> <p>The case of non-isotropic Gaussians can be found in the <a href="https://github.com/rfangit/analytical_flow_matching/blob/main/Optimal_Flow_Matching___Appendix.pdf" target="_blank">appendix, section 3.1.</a></p> <p>Recent work has noted that far away from the data manifold the trajectories move in straight curl-free paths. <d-cite key="balcerak2025energymatchingunifyingflow"></d-cite> I was curious if the optimal vector fields generated by flow-matching might always be conservative (and thus curl free) even close to the data manifold. If this was true, then the problem could be modeled via a time-dependent energy, which might simplify training.</p> <p>Sadly the optimal flow is not always conservative. This can be calculated explicitly by looking at the curl in 2D for a set of discrete data points $&gt;1$, in which one finds the curl increases at intermediate values of $t$ near the decision boundary that governs where points from Gaussian noise end up. Only when $t = 0, 1$ is the flow guaranteed to be nearly conservative, as the softmax simplifies.</p> <h2 id="closing-thoughts">Closing Thoughts</h2> <p>In my personal opinion, for well-defined problems that we know how to solve, we shouldn’t use learning algorithms like neural networks because they are slow, require lots of data and take a long time to train. A lot of problems in math, physics and computer science have these nice solutions. But there are a lot of ill-defined problems which we have no idea how to solve.</p> <p>Flow-matching caught my attention since the formalism is very simple - it can be understood in the language of 2nd year physics and ODEs, and there is indeed an optimal analytic solution. Unfortunately this solution doesn’t have the useful properties that ones produced by deep learning do.</p> <p>That said, I have ideas I’d like to try for making a more useful algorithm:</p> <ul> <li>The analytical solution depends on softmax probabilities, which rapidly shrink to 0 for far-away points. Instead of a full pass through the data per computation, it seems feasible to go through the data, index it with an algorithm like <a href="https://tylerneylon.com/a/lsh1/" target="_blank">locality sensitive hashing</a>, and then approximately compute it by only using close points.</li> <li>Recent work with locality suggests images can be generated by matching to patches of different scales <d-cite key="kamb2024analytictheorycreativityconvolutional"></d-cite> - patches were used in image generation <d-cite key="efros2023image"></d-cite> before deep learning arose (GANs), but the varying scales from diffusion seems like a new twist.</li> </ul> <p>More on these approaches hopefully in future posts or papers.</p> <p>On a side note, the optimal loss is very small at high $t$ values because the optimal flow instantly memorizes a specific point. This makes me wonder if it could serve as an indicator for the memorization regime, where a model that stops memorizing has significantly higher loss at high $t$ values. Would be nice to get a couple models in that regime and compare the loss vs t.</p> <h2 id="acknowledgements">Acknowledgements</h2> <p>My interest in this topic was heavily influenced by this blog post, <a href="https://drscotthawley.github.io/blog/posts/FlowModels.html" target="_blank">Flow With What You Know</a>, by Scott Hawley. Many kind thanks to Scott Hawley and Sander Dieleman for their generosity in answering questions and sharing background knowledge on this topic.</p> <p>If you find this post useful in your research or teaching, please consider citing it:</p> <pre><code class="language-bibtex">@misc{fan2025optimal,
  author = {Raymond Fan},
  title = {Optimal Flow-Matching},
  url = {https://rfangit.github.io/blog/2025/optimal_flow_matching/},
  year = {2025}
}</code></pre>]]></content><author><name>Raymond Fan</name></author><category term="code"/><category term="math"/><category term="artificial-intelligence"/><summary type="html"><![CDATA[The implementation of the solution for various datasets are available on github.]]></summary></entry><entry><title type="html">Do Conservative PINNs Train Faster In High Dimensions?</title><link href="https://raymondfan.github.io/blog/2025/conservative_pinns/" rel="alternate" type="text/html" title="Do Conservative PINNs Train Faster In High Dimensions?"/><published>2025-04-23T00:00:00+00:00</published><updated>2025-04-23T00:00:00+00:00</updated><id>https://raymondfan.github.io/blog/2025/conservative_pinns</id><content type="html" xml:base="https://raymondfan.github.io/blog/2025/conservative_pinns/"><![CDATA[<p>The code and experimental results for this post are available on <a href="https://github.com/rfangit/conservative_pinn_speed_test/" target="_blank">github.</a></p> <h2 id="introduction">Introduction</h2> <p><a href="https://arxiv.org/pdf/1906.01563v1" target="_blank"><strong>Hamiltonian Neural Networks</strong></a> are a type of physics informed neural network (PINN). Their predictions result in more physically accurate simulations since they conserve energy, making them attractive for physics purposes.</p> <p>They work by using coordinates $(x_1, x_2, …, x_n)$ to predict a scalar $U(x_1, …, x_n)$, and then automatic differentiation computes the gradients $\left(\frac{dU}{dx_1}, …, \frac{dU}{dx_n}\right)$ which are compared to training data. This differential structure guarantees its predictions will conserve energy, whereas a network that predict gradients directly generally does not conserve energy.</p> <p>In the original paper, the authors mentioned there were negligible differences in the test/train losses for a baseline NN and the Hamiltonian NN. It is only when the networks are used for a long time that the differences in energy conservation become apparent.</p> <p>But is that really true?</p> <h2 id="training-speed">Training Speed</h2> <p>In the original paper, experiments were done for mostly low-dimensional problems. The only case that the Hamiltonian NN showed significant improvement was also the only one with more than 2 system coordinates (the 2-body problem has 4 spatial and momentum coordinates, for a total of 8 coordinates).</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <img src="/assets/img/blog_2/train_test_losses.png" class="img-fluid rounded z-depth-1" width="80%" height="auto" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> <strong>Figure:</strong> Train and test losses, as well as energy conservation results from the original Hamiltonian Neural Network paper. Train and test losses between the baseline and the Hamiltonian network are similar except for the real pendulum and two-body problem. In the real pendulum, energy is not conserved so the conditions for a Hamiltonian are not satisfied. The two-body problem has a larger dimension than the other toy problems studied here ($d = 8$ with four spatial coordinates for the two bodies, and four velocity components, whereas the others only have 2 dimensions), which may explain the difference in it's performance. </div> <p>Hamiltonian NNs work by learning a single scalar function $U$ instead of $d$ separate gradient components. In theory, this suggests we should observe more significant speedups when the dimension of the problem is increased, which could explain the results in the previous paper.</p> <p>In practice, automatic differentiation could be too numerically unstable, or the scaling is too minor: it’s well known neural networks scale really well with high dimensions (growing polynomially instead of exponentially) so the effect of this differential structure may be too minor.</p> <p>In this blog post, we’ll test whether higher dimensions reveal clearer advantages in training speed through a toy conservative vector field problem.</p> <h2 id="experiment-design">Experiment Design</h2> <p>Our question: Does a network with the inductive bias of a conservative vector field built in via the gradient procedure in Hamiltonian Neural Networks train faster than a baseline network as dimensionality increases?</p> <h3 id="the-toy-problem">The Toy Problem</h3> <p>We construct a $d$-dimensional problem with inputs being coordinates $\vec{x} = (x_1, …, x_d)$ in $[-1,1]^d$, and a target output vector field $f(\vec{x})$ derived from a sum of Gaussians:</p> \[f(\vec{x}) = \sum_{i=1}^N -a_N \exp\left(-\frac{||\vec{x} - \vec{c}_N||^2}{\sigma_N^2}\right) (\vec{x} - \vec{c}_N)\] <p>Here $\vec{c}_N$ are randomly located Gaussian centers with amplitudes and widths $a_N, \sigma_N$. This vector field is conservative, since $f(\vec{x})$ is the gradient of a scalar sum of Gaussians.</p> <h3 id="network-architectures">Network Architectures</h3> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <img src="/assets/img/blog_2/neural_network.png" class="img-fluid rounded z-depth-1" width="80%" height="auto" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> <strong>Figure:</strong> A baseline network takes in $\vec{x}$ and outputs a $d$-dimensional vector directly as a prediction. Our conservative network instead predicts a scalar $U$, and it's predictions are obtained from taking the gradient of $U$, pictured above. </div> <p>We compare two approaches:</p> <ul> <li> <p><strong>Baseline Network:</strong><br/> Directly predicts all $d$ output components $(y_1, …, y_d)$.</p> </li> <li> <p><strong>Conservative Network:</strong><br/> Predicts a scalar potential $U(\vec{x})$, with gradients $\left(\frac{dU}{dx_1}, …, \frac{dU}{dx_n}\right) = \vec{y}$ computed via automatic differentiation.</p> </li> </ul> <p>Both use identical hidden-layer architectures, differing only in input/output dimensions. We analyze how the train and test losses behave as we scale the dimension of the problem.</p> <h3 id="scaling-with-dimension">Scaling with Dimension</h3> <h4 id="fixed-parameters">Fixed Parameters</h4> <ul> <li> <p><strong>Network Size</strong>: Hidden layers are fixed; Input and output(baseline network only) layers grow with $d$.</p> </li> <li> <p><strong>Test and Training Points</strong>: Fixed at $t_{test}, t_{train} = 10^4$. This reflects real-life high-dimensional problems, where training and test data is limited and does not grow with dimension.</p> </li> <li> <p><strong>Gaussian Amplitudes</strong>: All amplitudes are equal, fixed at 1.</p> </li> <li> <p><strong>Gaussian Variances</strong>: Variances are chosen uniformly from $[0.5, 1.0]$. This spread was chosen to make sure the different centers have different variances, and the variances are not so large as to cover all of space, nor so small as to make dense coverage of the space impossible without an exorbitant amount of points.</p> </li> </ul> <h4 id="three-experimental-regimes">Three Experimental Regimes</h4> <p>We test three approaches for the scaling underlying vector field with dimension, determined solely by the number of Gaussian centers.</p> <p><strong>Constant Density</strong>: As the dimension increases, the number of Gaussian points increase accordingly, via the following scaling law</p> \[N \propto \frac{V_{\text{cube}}}{\langle V_{\text{Gaussian}} \rangle} = \frac{2^d}{\langle \sigma^d \rangle} \cdot \frac{\Gamma\left(\frac{d}{2} + 1\right)}{\pi^{d/2}}\] <p>which approximately ensures each Gaussian <em>can</em> have 1 $\sigma$ of space around itself without overlaps. Note that due to random center generation, they will not end up evenly spread.</p> <p><strong>Fixed N = 15</strong>: As the dimension increases, the number of Gaussian points stays fixed at $N = 15$. This number was chosen arbitrarily.</p> <p><strong>Fixed N = 2</strong>: The number of Gaussian points stays fixed at $N = 2$. This was chosen to be simpler than the previous case, to observe how the problem difficulty affects the results.</p> <p>These three regimes span a wide variety of cases, from a problem whose complexity scales with dimension to a simple problem defined by the location of two points in $d$-dimensional space, and their corresponding variances.</p> <h2 id="hypothesis">Hypothesis</h2> <p>The conservative constraint should become more useful in high dimensions, since the baseline network must learn d independent functions, but the conservative network learns just one scalar potential.</p> <p>Therefore the conservative network should have lower train and test losses per epoch than the baseline network as the dimension of the problem is increased, regardless of the experimental regime.</p> <h2 id="results">Results</h2> <p>We tested networks across a variety of dimensions for the three experiments. For each dimension, three architectures were used:</p> <table> <thead> <tr> <th>Network Configuration</th> <th>Hidden Layers</th> </tr> </thead> <tbody> <tr> <td>Small</td> <td>[64, 64, 64]</td> </tr> <tr> <td>Medium</td> <td>[128, 128, 128]</td> </tr> <tr> <td>Large</td> <td>[256, 256, 256]</td> </tr> </tbody> </table> <p>Each architecture was trained with <strong>10 different weight initializations</strong> each on <strong>10 different randomly generated datasets</strong> to ensure reported results are not artifacts of peculiar initializations. All data (Gaussian parameters as well as training and test points) were generated with fixed random seeds for reproducibility.</p> <p>The train and test losses behaved almost identically for all architectures and datasets. All reported results occured regardless of weight initializations or dataset generation.</p> <h3 id="dense-gaussians">Dense Gaussians</h3> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <img src="/assets/img/blog_2/dense_256_losses.png" class="img-fluid rounded z-depth-1" width="80%" height="auto" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> <strong>Figure:</strong> Train and test losses for a network with hidden layers of $[256, 256, 256]$ neurons on our toy problem with densely packed Gaussians in $d = 2, 5, 8, 12$. Differences in losses gradually appear as the dimension increases. Shaded regions represent $\pm 1$ standard deviation among the 10 different weight initializations. </div> <p>In the dense case, little differences were observed between losses at $d &lt; 8$. At $d \geq 8$, there were notable decreases in the train/test loss for conservative networks. These trends occured in all network architectures, with the most significant effects for smaller networks.</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <img src="/assets/img/blog_2/dense_64_losses.png" class="img-fluid rounded z-depth-1" width="80%" height="auto" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> <strong>Figure:</strong> Train and test losses for a network with hidden layers of $[64, 64, 64]$ neurons on our toy problem with densely packed Gaussians in $d = 2, 5, 8, 12$. Differences in losses gradually appear as the dimension increases. The effect of conservative networks is notably larger in this smaller network. Shaded regions represent $\pm 1$ standard deviation among the 10 different weight initializations. </div> <h3 id="n--15-gaussians">N = 15 Gaussians</h3> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <img src="/assets/img/blog_2/sparse_losses.png" class="img-fluid rounded z-depth-1" width="80%" height="auto" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> <strong>Figure:</strong> Train and test losses for a network with hidden layers of $[64, 64, 64]$ and $[256, 256, 256]$ neurons on our toy problem with $N = 15$ Gaussians in $d = 5, 12, 20$. Slight differences in losses in higher dimensions, with benefits from conservative networks notably larger in smaller networks. Shaded regions represent $\pm 1$ standard deviation among the 10 different weight initializations. </div> <p>For fixed center number $N = 15$, there was little difference between between losses at small dimension, and notably smaller loss at higher dimensions. For fixed center number it is easy to increase the dimension of the problem, so we test up to $d = 20$. Smaller networks enjoy greater advantages from the conservative bias.</p> <p>Note the advantages of a conservative network are much smaller for the fixed center number than in the dense case.</p> <h3 id="n--2-gaussians">N = 2 Gaussians</h3> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <img src="/assets/img/blog_2/n=2_losses.png" class="img-fluid rounded z-depth-1" width="80%" height="auto" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> <strong>Figure:</strong> Train and test losses for a network with hidden layers of $[64, 64, 64]$ and $[256, 256, 256]$ neurons on our toy problem with $N = 2$ Gaussians in $d = 5, 12, 20$. Slight differences in losses in higher dimensions, with benefits from conservative networks notably larger in smaller networks. Shaded regions represent $\pm 1$ standard deviation among the 10 different weight initializations. </div> <p>For $N= 2$ Gaussians, the loss again only notably decreases at high dimensions, with effects more severe in smaller networks.</p> <h3 id="control-experiment-non-conservative-field">Control Experiment: Non-Conservative Field</h3> <p>To verify results arise from our conservative inductive bias, we test our networks on a non-conservative field. This field was achieved by multiplying our conservative vector field outputs by a fixed matrix of constant coefficients.</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <img src="/assets/img/blog_2/nonconservative_losses.png" class="img-fluid rounded z-depth-1" width="80%" height="auto" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> <strong>Figure:</strong> Train and test losses for baseline and conservative neural networks when the underlying problem is not a conservative vector field. The vector field was generated via a linear transformation on the original conservative vector field, which is known to be enough to break conservativity. The conservative neural network does noticeably worse in all cases. </div> <p>The performance of conservative networks degraded significantly here. showing that our observed improvements in training speed occur because our conservative networks are well-suited to predicting conservative vector fields.</p> <p>More data for other dimensions, other initializations of datasets and the intermediate architecture size can be found on the github repo.</p> <h2 id="discussion">Discussion</h2> <p>Hamiltonian networks contain inductive biases inspired from physics, and their advantages in accurately simulating physical systems are well established.</p> <p>This work instead examined whether networks with a built-in conservation law (such as Hamiltonian NNs) improved training speed. The results showed these networks have <strong>smaller training and test loss for a given epoch when the dimension of the problem is very high.</strong> This is consistent with results from the original, where the only system to show improvements in training and test loss was the two body problem, which has 8 system coordinates (4 spatial coordinates and 4 velocity coordinates). However, the reduction in loss was fairly minor, with baseline networks almost always achieving lower loss when given x3 the number of epochs.</p> <p>This aligns with the theoretical expectation that learning a scalar potential should be simpler than a $d$ diemsnional vector field. Unfortunately, there is no evidence of a general scaling law for the training speedup of a conservative network, as the speedup effects are dependent on the architecture and do not appear to generalize. See <a href="https://github.com/rfangit/conservative_pinn_speed_test/blob/main/figs/Dense%20Experiments/figs/12D_loss_ratio_64_64_64.png" target="_blank">example</a> <a href="https://github.com/rfangit/conservative_pinn_speed_test/blob/main/figs/Dense%20Experiments/figs/12D_loss_ratio_256_256_256.png" target="_blank">figures</a> on the ratio of test losses in the github repo.</p> <p>In this comparison I’ve neglected the additional compute time cost of the conservative network. The conservative network generates a scalar, and automatic differentiation is needed for gradients. Automatic differentiation is also used in computing the loss, and constitutes a significant amount of time in training networks.</p> <p>In our experiments, this makes our conservative networks take roughly twice as long per epoch. Plotting the compute time instead of epoch number, we find very minor gains in training performance assuming they do not disappear entirely.</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <img src="/assets/img/blog_2/compute_time.png" class="img-fluid rounded z-depth-1" width="80%" height="auto" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> <strong>Figure:</strong> Taking into account the automatic differentiation needed to evaluate conservative networks roughly doubles the time per epoch, we compare conservative networks with the baseline. There are some slight advantages even in the larger networks, but the increased computational cost removes most advantages in training speed. </div> <p><strong>Note:</strong> It’s not clear to me that this slowdown is fundamental and can’t be sped up with clever gradient accumulation, so this problem may be fixable.</p> <p>These findings suggest the gains by a conservative architecture in speeding up training speeds is relatively modest, especially when training large networks. The advantages are larger for smaller networks.</p> <h2 id="closing-thoughts">Closing Thoughts</h2> <p>Although the original paper on Hamiltonian Neural Networks focused on many simple low-dimensional problems, many interesting problems in physics are very high dimensional. Since the conservative vector field bias built-in Hamiltonian Neural Networks intrinsically scales with dimension, I thought they could turn out to be extemely useful if they scaled very well.</p> <p>For example, convolutional neural networks make use of translational invariance and demonstrate large advantages in tasks with such symmetries (eg, most computer vision tasks). While such speedups would be unlikely for Hamiltonian Neural Networks (the conservative constraint seems much weaker than translational invariance), it does leave some hope that they might scale well in a meaningful way.</p> <p>Sadly, our results show that while speed benefits appear in higher dimensions, they are relatively modest. As an (unproven) explanation, it seems the natural ability of neural networks to perform extremely well in high dimensions suppresses the modest dimensional scaling of a conservative vector field. Furthermore, difficulties with multiple uses of automatic differentiation make the modest benefits in training speed fairly negligible, barring the most extreme cases.</p> <p>If you found the contents of this blog post useful, or have any questions, please feel free to leave a comment.</p>]]></content><author><name>Raymond Fan</name></author><category term="code"/><category term="math"/><category term="artificial-intelligence"/><summary type="html"><![CDATA[Alternatively, how strong is a conservative vector field as an inductive bias?]]></summary></entry><entry><title type="html">How Molecular Cascades Communicate</title><link href="https://raymondfan.github.io/blog/2025/info_cascades/" rel="alternate" type="text/html" title="How Molecular Cascades Communicate"/><published>2025-04-09T00:00:00+00:00</published><updated>2025-04-09T00:00:00+00:00</updated><id>https://raymondfan.github.io/blog/2025/info_cascades</id><content type="html" xml:base="https://raymondfan.github.io/blog/2025/info_cascades/"><![CDATA[<h2 id="introduction">Introduction</h2> <p>Information theory was originally developed in the context of optimizing electronic communications. This has resulted in much confusion when applied to the biological sciences.</p> <p>This blog post is intended to clarify what information theory tells us about information transfer through a chemical cascade. It is intended as a supplement to <a href="https://arxiv.org/pdf/2309.10162" target="_blank">my paper on the topic</a>, intended for an audience new to both information theory and stochastic modelling.</p> <p>Specifically, we analyze a cascade of three species of molecules, $X \xrightarrow{} Y \xrightarrow{} Z$, where $X \xrightarrow{} Y$ denotes that the production rate of molecule $Y$ is proportional to the amount of $X$ molecules, and similarly for $Y \xrightarrow{} Z$. This represents a simple biochemical cascade.</p> <p>Mathematically, the cascade is defined by the following chemical reactions</p> <p>\begin{aligned} y &amp;\xrightarrow[\phantom{y/\tau_y}]{\alpha x} y+1 &amp; <br/> y &amp;\xrightarrow[\phantom{y/\tau_y}]{y/\tau_y} y-1 &amp; <br/> z &amp;\xrightarrow[\phantom{z/\tau_z}]{\beta y} z+1 &amp; <br/> z &amp;\xrightarrow[\phantom{z/\tau_z}]{z/\tau_z} z-1 &amp; \end{aligned}</p> <p>where the dynamics governing $x(t)$ have been left unspecified. Such cascades have been used before to model <a href="https://www.theorie.physik.lmu.de/lsfrey/teaching/archiv/wise_08_09/statphysbio/Paulsson_Physics_of_Life_Reviews_2005.pdf" target="_blank">relationships between DNA, mRNA and protein</a>.</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <img src="/assets/img/blog_1/time_traces.png" class="img-fluid rounded z-depth-1" width="80%" height="auto" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> The chemical reactions can be simulated for some input $X(t)$ to generate $Y(t), Z(t)$. The process is fundamentally stochastic, resulting in different traces with each simulation. </div> <h2 id="information-theory-basics">Information Theory Basics</h2> <p>This section is an introduction to information theory concepts for non-experts. Those who are already familiar with information theory can skip ahead to the next section.</p> <p>To communicate through our channel, we can choose an input signal $X(t)$, and the cascade will generate signals $Y(t), Z(t)$. An observer watching the behavior of $Y(t), Z(t)$ can identify characteristics of the chosen input $X(t)$ and receive information.</p> <p>This idea is formalized in information theory by the mutual information between a pair of variables $X, Y$, given by the following formula</p> \[I(X; Y) = H(Y) - H(Y|X)\] <p>where $ H(Y) $ denotes the entropy of $ P(Y) $, while $ H(Y | X) $ is the entropy of the conditional distribution $ P(Y | X) $. The entropy is given by</p> \[H(X) = \sum_x P(x) log(P(x)) .\] <p>Entropy is an important concept in information theory, and measures spread of a distribution.</p> <p>To understand the idea behind this formula, consider a communication channel that returns $Y$ given $X$. In an ideal channel, every input $X$ maps to a unique output $Y$, and thus $H(Y | X) = 0$. Then mutual information is maximized at $I(X; Y) = H(Y)$. Most channels are noisy with $H(Y | X) &gt; 0$, lowering the information shared across this channel.</p> <p>The ‘noise’ also depends on the input distribution $P(X)$. Some inputs, when sent throught he channel, result in outputs that are easier to distinguish than others.</p> <p>The channel capacity is the maximum $I(X; Y)$, achieved by maximizing over all possible input distributions $P(X)$. It quantifies the maximum amount of information that can be sent through the channel.</p> <p>A useful property of mutual information is that it obeys the data processing inequality. The inequality states for a Markov chain $X \xrightarrow{} Y \xrightarrow{} Z$,</p> \[I(X; Z) \leq I(X; Y), I(Y; Z).\] <p>Colloquially, this means mutual information between distant variables in the chain is always less than mutual information between closely related variables. The inequality is actually true as long as $P(X, Z |Y) = P(X | Y)P(Z | Y)$ (known as <b>conditional independence </b>), which is true for all Markov chains $X \xrightarrow{} Y \xrightarrow{} Z$.</p> <p>These properties imply we can analyze large communication networks by computing the channel capacity of individual channels. The data processing inequality tells us the channel capacity of the entire channel is given by the smallest channel capacity in the network.</p> <p><strong>Note:</strong> In the context of electrical communications, the channel capacity for a large network is achievable by transforming the data appropriately for each channel. But for a biological cascade where components are linked up directly and arbitrary transformations cannot be applied, the actual channel capacity may be much less than the individual components.</p> <h2 id="chemical-cascades-and-information">Chemical Cascades and Information</h2> <p>Our cascade consists of physical processes that transform inputs $X$ into outputs $Y$ from a distribution $P(Y | X)$, and is thus a channel with nonzero error rate. The input to the cascade is a time-varying signal $X(t)$, and the output is a time-varying signal $Y(t), Z(t)$.</p> <p>For time-varying signals, channels are usually analyzed in terms of information transfer rates</p> \[R(X(t);Y(t)) = \frac{I(X(t); Y(t))}{D}\] <p>where $D$ denotes the duration over which we use our channel. The amount of information that can be conveyed through our channel increases linearly with the duration of usage $D$. This captures the idea that we can send arbitrarily large amounts of information through poor communication channels given enough time.</p> <p>To transfer information in our cascade, select an input time trace $X(t)$ of duration $D$. The physical processes of our channel convert the input into output time traces $Y(t), Z(t)$ with associated probabilities $P(Y(t) | X(t)), P(Z(t) | X(t))$. Channel quality is measured by rates $R(X(t);Y(t)), R(X(t);Z(t))$.</p> <p>For our chemical cascade, $P(Z(t) | Y(t), X(t)) = P(Z(t) | Y(t))$ since the time trace of $Z(t)$ only depends on $X(t)$ through how it affects $X(t)$ by definition, so the data processing inequality holds for the rates.</p> <h3 id="distinct-channels-in-our-cascade">Distinct Channels In Our Cascade</h3> <p>To determine information rates, note our cascade consists of four physical processes that generate new time traces given the preceding result as input.</p> <p>The first process is</p> <p>\begin{aligned} y \xrightarrow{\alpha x} y+1 \end{aligned}</p> <p>which from input time trace $X(t)$, generates a time trace $Y_{birth}(t)$ that tracks all the $y$ molecule production events that occur over the interval of usage $D$.</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <img src="/assets/img/blog_1/process_one.png" class="img-fluid rounded z-depth-1" width="90%" height="auto" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> Example input and output time traces for the channel given by the molecular production reaction. </div> <p>The second process is</p> <p>\begin{aligned} y \xrightarrow{y/\tau_y} y-1 \end{aligned}</p> <p>which uses $Y_{birth}(t)$ and an initial number of $y$ molecules, and generates the time trace $Y(t)$ using the random degradation of y molecules.</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <img src="/assets/img/blog_1/process_two.png" class="img-fluid rounded z-depth-1" width="90%" height="auto" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> Example input and output time traces for the channel given by the molecular degradation reaction. </div> <p>The other two physical processes are</p> <p>\begin{aligned} z &amp;\xrightarrow[\phantom{z/\tau_z}]{\beta y} z+1 &amp; <br/> z &amp;\xrightarrow[\phantom{z/\tau_z}]{z/\tau_z} z-1 &amp; \end{aligned}</p> <p>which generate $Z_{birth}(t)$ from $Y(t)$, and then $Z(t)$ using $Z_{birth}(t)$ births and an initial $Z(t = 0)$ value of molecules.</p> <h3 id="capacity-of-the-distinct-channels">Capacity Of The Distinct Channels</h3> <p>Our cascade consists of two types of processes, production events and degradation events. Here we discuss the capacity of these processes.</p> <p>The first process</p> <p>\begin{aligned} y \xrightarrow{\alpha x} y+1 \end{aligned}</p> <p>corresponds to a well studied channel in the optical communications literature, known as the direct detection photon channel. Several results for the channel capacity are known under different constraints for $\alpha x$, which we briefly include here.</p> <details><summary>Channel Capacity Under Different Input Constraints</summary> <p>These results are taken from this <a href="https://static-content.springer.com/esm/art%3A10.1038%2Fnature09333/MediaObjects/41586_2010_BFnature09333_MOESM273_ESM.pdf">supplement</a>. If $\alpha x \leq A_{max}$, then it is known that the capacity is</p> \[C = A_{max} / e\] <p>If the mean $\alpha \langle x \rangle$ is also constrained, then the capacity becomes</p> \[C = \alpha \langle x \rangle \log \left( \frac{A_{max}}{\alpha \langle x \rangle} \right)\] <p>These capacities are realized by sending in $x(t)$ that oscillates between 0 and $A_{max}$ instantaneously. This is highly nonphysical for biological systems.</p> <p>The first capacity can be proven by breaking up the usage of the channel for duration $D$ into using the channel $N$ times each for a duration $D/N$. For large $N$ each use of the channel is equivalent to a Z channel with an error rate proportional to $N$, and taking the limit $N \xrightarrow{} \infty$ yields the capacity above. The optimal input time trace follows from the optimal inputs to the Z channel.</p> <p>Another constraint has been derived when the first two moments of the input signal are fixed, yielding</p> \[C = \alpha \langle x \rangle \log \left(1 + \frac{\sigma^2_f}{\langle x \rangle^2}\right) \leq \frac{\sigma^2_f}{\langle x \rangle}\] </details> <p>For the second process</p> <p>\begin{aligned} y \xrightarrow{y/\tau_y} y-1 \end{aligned}</p> <p>which uses $Y_{birth}(t)$ and an initial number of molecules to generate $Y(t)$, observe $Y_{birth}(t)$ can always be accurately inferred from $Y(t)$, since molecular production events are easily detectable. Therefore the channel capacity of this process is always the entropy of the input $Y_{birth}$.</p> <p>Thus the channel capacity of $X \rightarrow Y$ is the channel capacity of the production reaction, which has been studied before for various constraints.</p> <p>Conditional independence between $X(t), Z(t) | Y(t)$ thus implies the channel capacity of the full cascade is</p> \[C_{X\rightarrow Y \rightarrow Z} = \textbf{min}(C_{X\rightarrow Y }, C_{Y\rightarrow Z })\] <h2 id="existing-biological-experimental-data">Existing Biological Experimental Data</h2> <p>In practice, it is difficult to measure time traces of multiple molecules inside of living cells simultaneously. Furthermore, the mutual information discussed above applies to distributions of time traces, which is hard to estimate from experimental data (any specific time trace has near zero probability).</p> <p>However, it is possible to measure the abundances of multiple molecules in recently deceased cells. This can be done on large numbers of cells to obtain joint probability distributions of <b>instantaneous</b> molecular abundances, illustrated in the following figure. This data can be analyzed to infer properties of the underlying relationships.</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <img src="/assets/img/blog_1/full%20process.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> The chemical reactions inside of a living cell result in a channel of input and output time traces, shown on the top. But typical experiments only measure instantaneous molecular abundances, which are repeated for many different cells to obtain joint distributions of instantaneous abundances, illustrated on the bottom. These instantaneous abundances do not form a channel with respect to each other. </div> <p>Note instantaneous joint distributions do not obey the same properties as the underlying joint distributions of time traces. Instead, they are derived from the time trace distribution through randomly sampling the time traces at the same time.</p> <p>Such joint distributions still contain useful information theoretic properties. Consider the probability distribution of time traces $P(X(t))$. From this we derive both the sequence of birth events $Y_{birth}$ (and thus the time trace $Y(t)$), and the probability distribution of instantaneous $X$ values. Thus we find $X, Y(t)$ are conditionally independent of each other given $X(t)$.</p> <p>However, the link between the different instantaneous distributions only arises from averaging the related time traces. The different instantaneous values of $X, Y, Z$ do not form a proper communication channel $X \rightarrow Y \rightarrow Z$, and thus $X, Z$ are not conditionally independent of each other given $Y$ - they become independent only when conditioned on $X(t), Y(t)$ or $Z(t)$.</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <img src="/assets/img/blog_1/markov_chain.png" class="img-fluid rounded z-depth-1" width="80%" height="auto" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> A source of confusion for applying the data processing inequality is the known fact it works for Markov chains. A cascade of chemical reactions such as the one analyzed in this post is in fact a Markov chain, but not between the variables $X \rightarrow Y \rightarrow Z$. If the state of the cell is treated as a variable (a vector with the abundances of all molecules involved, shown on the right) then the state of the cell forms a Markov chain between the different states in time. </div> <h3 id="violations-of-the-data-processing-inequality">Violations Of The “Data-Processing Inequality”</h3> <p>A consequence of analyzing the static distributions from experiments is that the data processing inequality is no longer true. This is because the variables $X, Y, Z$ do not form a channel.</p> <p>This allows for biological cascades to generate distributions in experiments that violate the inequality. This was covered in detail in the paper, where mathematics and numerical simulations have been used to explore the regime of parameter space for which these inequalities break down. This is illustrated in the following figure, where simulating a cascade for a variety of parameters reveals regions of parameter space for which the mutual information $I(X; Z) &gt; I(X; Y)$ (blue region).</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <img src="/assets/img/blog_1/mutual_information_figure.png" class="img-fluid rounded z-depth-1" width="80%" height="auto" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> Simulating the cascade for a $X(t)$ that obeys a poissonian birth-death process for a wide variety of parameters allows us to obtain the joint distributions of the instantaneous abundances, $P(X, Y, Z)$. Calculating the mutual information for these joint distributions for a wide range of parameters reveals regimes where the inequalities suggested by the data processing inequality are violated (blue region). See the original paper for more details. </div> <p>For a simple intuitive explanation of how this can occur, consider the problem of estimating the instantaneous value of $X$. Suppose the abundance of $Y$ is very small, such that low copy number noise is very significant. Then for any value of $X$, there is a large variety of $Y$ values, and it is difficult to estimate $X$ from $Y$.</p> <p>Suppose $Y$ molecules have a very short lifetime compared to $X, Z$ molecules. Then the abundance $Y$ will oscillate very quickly. $Z$ molecules, which are made proportional to the amount of $Y$ molecules, will experience rapid fluctuations in their birth rate. But the $Z$ abundance is related to the number of $Z$ molecules made over the average lifetime of a $Z$ molecule, and thus does not vary much if the $Z$ lifetime is large compared to the $Y$ molecule lifetime.</p> <p>In this way, we can accurately estimate the current value of $X$ by looking at the $Z$ abundance, since $Z$ time-averages out the large amount of $Y$ noise. This idea of estimation is closely linked with the mutual information (which represents the reduction in uncertainty about the value of $X$).</p> <h2 id="on-actual-inequalities">On Actual Inequalities</h2> <p>In this post, we’ve seen how the data processing inequality applies to chemical cascades only when considering the distribution of time traces.</p> <p>The often experimentally measured instantaneous distributions of molecules however, do not obey these inequalities because they do not form a Markov Chain with respect to each other, and there exist parameter regimes where even simple cascades can display apparent ‘violations’.</p> <p>This observation prompts a deeper question:</p> <blockquote style="color: inherit; border-left: 3px solid #ccc; padding-left: 15px;"> Can we derive <b>new inequalities</b> that apply to these experimentally accessible (static) distributions? </blockquote> <p>Surprisingly, <b>yes</b>—but with caveats.</p> <p>While we saw that $Y$ can accumulate arbitrary information from $X$ over time, accurate information about the current state of $X$ is different, as older information becomes irrelevant. There is a competition between:</p> <ul> <li>The <b>rate</b> at which $Y$ learns about $X$ (governed by the production reaction)</li> <li>The <b>rate</b> at which $X$’s state changes (governed by X’s dynamics)</li> </ul> <p>Unlike the DPI, these new inequalities are dependent on the dynamics of the system involved and do not generalize. Furthermore, they’re mathematically difficult to derive and work with.</p> <p>I’ll discuss more on this topic in more detail in a future blog post, on <b>“Inequalities for Static Distributions in Biochemical Cascades”</b>.</p>]]></content><author><name>Raymond Fan</name></author><category term="biology"/><category term="information-theory"/><category term="math"/><summary type="html"><![CDATA[Clarifying a common misunderstanding when applying information theory to biological data]]></summary></entry></feed>